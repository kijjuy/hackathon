@using Microsoft.AspNetCore.Identity;
@inject UserManager<ApplicationUser> userManager;

@{
    ViewData["Title"] = "Home Page";
}

@{
    var user_from_manager = await userManager.GetUserAsync(User);
    var description = user_from_manager.Description;
}

<div class="text-center">
    <h2>Update your profile:</h2>
    <form asp-controller="Home" asp-action="UpdateProfile" target="_self" enctype="application/x-www-form-urlencoded">
    @Html.AntiForgeryToken
    <div class="form-group">

    </div>
    </form>
</div>
